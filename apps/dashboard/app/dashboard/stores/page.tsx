import { PageShell } from "@/components/page-shell";

export default function StoresPage() {
  return (
    <PageShell title="Stores CRUD">
      <p className="text-slate-600">Manage store identity, policies, and channel installs per tenant.</p>
    </PageShell>
  );
}
